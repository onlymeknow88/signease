# Workspace Rules

- **Do NOT run `npm run build`**: The user does not want the workspace environment to execute `npm run build` or similar build verification commands. Verify code correctness by checking imports, TypeScript syntax, and running lighter dev-mode validations if necessary.
